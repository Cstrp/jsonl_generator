import emotionStyled from '@emotion/styled';

export const PairCounter = emotionStyled.span`
  color: var(--text-secondary);
  font-size: 0.875rem;
  padding: 0.5rem 1rem;
  background: var(--primary);
  border-radius: 8px;
  border: 1px solid var(--border);
`;
